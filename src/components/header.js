const Header = () => {
    return (
        <nav className="topnav">
            <a className="active" href="home">Home</a>
            <a href="cart">Cart</a>
        </nav>
    );
}

export default Header